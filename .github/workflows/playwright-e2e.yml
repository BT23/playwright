
name: E2E

on:
  push:    
    branches:
      - ci-test-branch
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
        E2E_USERNAME: ${{ secrets.E2E_USERNAME }}
        E2E_PASSWORD: ${{ secrets.E2E_PASSWORD }}
        BASE_URL: https://staging-2026-01-15.mex16.dev/

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.10.0
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      # Run feature tests for work orders and assets
      - name: Run feature tests (WO + Asset)
        run: npm run test:features
       
      # Always upload raw test-results if your tests output there (e.g. trace, screenshots)
      - name: Upload Playwright test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
            name: test-results
            path: test-results
            if-no-files-found: ignore

      # Optional: Generate HTML report (for smoke subset)
      - name: Upload HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report             
          path: playwright-report
