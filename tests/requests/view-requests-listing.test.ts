import { test } from '../fixtures';

    /*
    * Reference ID: Test Case: Requests Module should open successfully
    * Preconditions: User is logged in
    * Steps:
    * 1. Open Requests Module
    * 2. Verify that the Requests Module header is displayed
    * Expected Result: Requests Module loads without errors
    * Custom tags: @smoke
    */
    
    // For Debug purposes only - to be uncommented when needed
    //test('Open Requests Module using fixture data @smoke @feature-request', async ({ requestPage, loginPage }) => {
    //    await requestPage.openRequestsModule();
    //});